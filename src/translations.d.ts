export namespace en {
    namespace RunnerUploader {
        const addFiles: string;
        const emptyHeadingText: string;
        const emptySubheadingText: string;
        const error: string;
        const incoming: string;
        const upload: string;
        const uploadComplete: string;
        const uploadMore: string;
        const uploading: string;
        namespace edge {
            const addFiles_1: string;
            export { addFiles_1 as addFiles };
            const emptyHeadingText_1: string;
            export { emptyHeadingText_1 as emptyHeadingText };
            const emptySubheadingText_1: string;
            export { emptySubheadingText_1 as emptySubheadingText };
            const error_1: string;
            export { error_1 as error };
            const incoming_1: string;
            export { incoming_1 as incoming };
            const upload_1: string;
            export { upload_1 as upload };
            const uploadComplete_1: string;
            export { uploadComplete_1 as uploadComplete };
            const uploadMore_1: string;
            export { uploadMore_1 as uploadMore };
            const uploading_1: string;
            export { uploading_1 as uploading };
        }
    }
    namespace RunnerThumbnailUpdater {
        export const addFile: string;
        const emptyHeadingText_2: string;
        export { emptyHeadingText_2 as emptyHeadingText };
        const emptySubheadingText_2: string;
        export { emptySubheadingText_2 as emptySubheadingText };
        export const emptyAdditionalText: string;
        const error_2: string;
        export { error_2 as error };
        export const fileReadFailed: string;
        const incoming_2: string;
        export { incoming_2 as incoming };
        export const loading: string;
        const upload_2: string;
        export { upload_2 as upload };
        const uploadComplete_2: string;
        export { uploadComplete_2 as uploadComplete };
        const uploading_2: string;
        export { uploading_2 as uploading };
        export const tooMany: string;
        export const invalidFile: string;
    }
}
